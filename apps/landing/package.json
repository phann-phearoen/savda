{
  "name": "landing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:locales": "node scripts/build-locales.mjs",
    "dev": "pnpm run build:locales && vite",
    "build": "pnpm run build:locales && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@savda/tokens": "workspace:*"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
